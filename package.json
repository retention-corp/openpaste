{
  "name": "openpaste-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check formatter.js && node --check tests/formatter.test.js",
    "test": "node --test"
  }
}
